name: transparent
in: query
description: Background transparency of map (defaults to `true` when `bgcolor` is specified, but to `false` when a `bgcolor` is used).
required: false
style: form
explode: false
schema:
  type: boolean
  default: true
